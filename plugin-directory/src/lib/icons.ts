import { Activity, Database, HardDrive, Rabbit, Server, Workflow } from "lucide-react";
import type { LucideIcon } from "lucide-react";

export const iconMap: Record<string, LucideIcon> = {
  activity: Activity,
  database: Database,
  "hard-drive": HardDrive,
  rabbit: Rabbit,
  server: Server,
  workflow: Workflow,
};
